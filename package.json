{
  "name": "electron-autoupdate-demo",
  "version": "0.1.0",
  "description": "Electron auto-update demo with electron-builder and electron-updater",
  "private": true,
  "main": "src/main.js",
  "author": "",
  "license": "MIT",
  "scripts": {
    "postinstall": "node node_modules/electron/install.js",
    "start": "electron .",
    "dev": "cross-env NODE_ENV=development electron .",
    "build": "electron-builder",
    "release": "electron-builder --publish always",
    "release:it": "release-it",
    "changelog": "release-it --no-git.tag --no-git.commit --no-git.push --no-increment --github.release false",
    "test": "vitest run"
  },
  "dependencies": {
    "electron-log": "^5.4.3",
    "electron-updater": "^6.6.2"
  },
  "devDependencies": {
    "@release-it/conventional-changelog": "^7.0.2",
    "cross-env": "^10.1.0",
    "electron": "^39.2.3",
    "electron-builder": "^26.0.12",
    "release-it": "^17.11.0",
    "vitest": "^4.0.13"
  },
  "build": {
    "appId": "com.example.electronautoupdatedemo",
    "productName": "Electron AutoUpdate Demo",
    "files": [
      "src/**/*",
      "package.json"
    ],
    "asar": true,
    "directories": {
      "buildResources": "build"
    },
    "win": {
      "target": [
        "nsis"
      ],
      "artifactName": "${productName}-${version}-${os}-${arch}.${ext}"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "wood3n",
        "repo": "electron-autoupdate-demo",
        "releaseType": "release"
      }
    ]
  },
  "release-it": {
    "git": {
      "requireBranch": "main",
      "requireCleanWorkDir": true,
      "tagName": "v${version}",
      "commitMessage": "chore: release v${version}",
      "push": true
    },
    "github": {
      "release": true,
      "tokenRef": "GITHUB_TOKEN"
    },
    "npm": {
      "publish": false
    },
    "plugins": {
      "@release-it/conventional-changelog": {
        "preset": "angular",
        "infile": "CHANGELOG.md",
        "header": "# Changelog",
        "releaseCount": 1
      }
    }
  }
}
